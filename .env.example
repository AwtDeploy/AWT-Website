CMS_USERNAME=awtadmin
CMS_PASSWORD=change-me
CMS_SESSION_SECRET=change-this-secret

# PostgreSQL only (free tier is enough). Leave empty to use local data/jobs.json.
# Local: postgresql://postgres:postgres@localhost:5432/awt_website
# Free hosted: Neon (neon.tech) or Supabase (supabase.com) — copy the URI into DATABASE_URL.
DATABASE_URL=
